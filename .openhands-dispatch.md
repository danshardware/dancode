CONTEXT: You are running inside a dedicated git worktree for a single coding task. The worktree is already on the correct branch for this task.

HARD CONSTRAINTS — you MUST follow these at all times:
• Do NOT run git branch, git checkout, git switch, git worktree, or any command that creates or changes branches.
• Commit changes directly to the current branch (no branch switching).
• Do NOT concern yourself with changes from other tasks — focus only on the single task described below.
• Do NOT add, remove, or modify files outside the scope of this task.

---
TASK: Wire Restart into Task Detail + App
SOURCE PLAN: Planning/usability-improvements/track-b/005-restart-app-wiring.md
---

Read the source plan file listed above. Then:

2. Read every file listed in the "Files Changed" section of the plan.
   Do not edit anything yet — understand first.

3. Implement the changes described in the "Workflow" section step by step.
   After each numbered step, stop and verify the change compiles / imports cleanly.

4. Commit with message: "005-restart-app-wiring: Wire Restart into Task Detail + App — initial implementation"

5. Write the tests described in "Testing Plan". Run them. Fix any failures.

6. Commit with message: "005-restart-app-wiring: Wire Restart into Task Detail + App — tests passing"

7. Verify every item in "Acceptance Criteria" is satisfied. For each item,
   output the assertion or command and its result.

8. If any criterion fails, fix it now. Do not move on with a failing criterion.

9. Final commit with message: "005-restart-app-wiring: Wire Restart into Task Detail + App — complete"

Do not proceed to the next task. Stop here and report: DONE or BLOCKED: <reason>.
---
